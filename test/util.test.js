import * as util from '../nodeuii/src/util.js';

import chai from 'chai';
var assert = chai.assert;

describe('getCurrentQuarter测试', function () {
  it('getCurrentQuarter测试对吗？', function () {
    assert.typeOf(util.transformArray, 'function');
  });
});
